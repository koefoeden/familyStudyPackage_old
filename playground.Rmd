```{r}
top_50_most_common_phenotypes()
```

```{r}
search_for_phenotype("glu")
```


```{r}
combined_pheno_data <- get_combined_pheno_data()

combined_pheno_data %>% 
  ggplot(aes(x=ISI, color=insres_status, 
             group=insres_status,
             y=insres_status), alpha=0.1) +
  geom_boxplot() +
  geom_point(aes(fill=insres_status)) +
  facet_wrap(~sex)


```

```{r}
combined_pheno_data %>% 
  ggplot(aes(x=bmi, y=ISI, color=sex),
         alpha=0.1) +
  geom_point() +
  geom_smooth(method = "lm")
```

```{r}
combined_metadata <- get_combined_pheno_data()
combined_metadata$famnum

```

